function checkAnswer(answer) {
    var correctAnswer = "Це дерево";
  
    if (answer === correctAnswer) {
      alert("Відповідь правильна!");
    } else {
      alert("Відповідь неправильна. Спробуйте ще раз.");
    }
  }
